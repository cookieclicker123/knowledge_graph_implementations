[pytest]

# Asyncio configuration
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function

# Filter warnings
# Ignore the deepeval version warning
filterwarnings =
    ignore:You are using deepeval version.*:UserWarning 